/* eslint-disable react/prop-types */
import React from 'react';

const ReviewRatingBar = ({ n, metadata }) => (
  <div className='reviewSingleFilterBar'>review bar here</div>
);

export default ReviewRatingBar;
